import EntityApprovalStatus from './EntityApprovalStatus';

class EncounterEntityApprovalStatus extends EntityApprovalStatus {
  constructor(that = null) {
    super(that);
  }
}

export default EncounterEntityApprovalStatus;
