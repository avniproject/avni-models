import EntityApprovalStatus from './EntityApprovalStatus';

class SubjectEntityApprovalStatus extends EntityApprovalStatus {
  constructor(that = null) {
    super(that);
  }
}

export default SubjectEntityApprovalStatus;
