import EntityApprovalStatus from './EntityApprovalStatus';

class ProgramEncounterEntityApprovalStatus extends EntityApprovalStatus {
  constructor(that = null) {
    super(that);
  }
}

export default ProgramEncounterEntityApprovalStatus;
